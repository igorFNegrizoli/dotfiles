[shell]
# Opens tmux with new session
# Session pool shared and mirrored between alacritty terminals
program = "/bin/sh"
args = ["-c", "tmux new-session -t main || tmux new-session -s main"]


[[keyboard.bindings]]
key = "Return"
mods = "Shift"
chars = "\u001b\r"

